<IfModule mod_rewrite.c>
  RewriteEngine On
  # Redirect all requests to the Node.js server running on port 4000
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ http://localhost:4000/$1 [P,L]
</IfModule>
